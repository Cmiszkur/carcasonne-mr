import { TileBorderColorPipe } from './tile-border-color.pipe';

describe('TileBorderColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TileBorderColorPipe();
    expect(pipe).toBeTruthy();
  });
});
