import { EmptyTileClickDirective } from './empty-tile-click.directive';

describe('EmptyTileClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyTileClickDirective();
    expect(directive).toBeTruthy();
  });
});
