import { PlacedTilePayloadPipe } from './placed-tile-payload.pipe';

describe('PlacedTilePayloadPipe', () => {
  it('should be defined', () => {
    expect(new PlacedTilePayloadPipe()).toBeDefined();
  });
});
